# Setup and Run Guide

## 📋 Quick Start

### Step 1: Train the Models (Backend)

```bash
cd Backend

# Train all custom models
python train_yield_model.py
```

**Expected Output:**

- `models/yield_model.joblib` + `meta_yield.json`
